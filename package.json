{
  "name": "tauri-app",
  "scripts": {
    "tauri": "tauri",
    "dev:app": "next build && next export && yarn run tauri dev",
    "build:app": "next build && next export && yarn run tauri build",
    "dev": "next dev",
    "build": "next build",
    "lint": "eslint --ignore-path .gitignore ."
  },
  "dependencies": {
    "@tauri-apps/api": "^1.2.0",
    "next": "^12.2.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.2.2",
    "@types/node": "^18.11.18",
    "@types/react": "^18.0.26",
    "@types/react-dom": "^18.0.10",
    "eslint": "^8.31.0",
    "eslint-config-next": "^13.1.1",
    "typescript": "^4.9.4"
  }
}
