{
  "name": "tauri-app",
  "scripts": {
    "tauri": "tauri",
    "dev": "next build && next export && yarn run tauri dev",
    "build": "next build && next export && yarn run tauri build",
    "lint": "eslint --ignore-path .gitignore ."
  },
  "dependencies": {
    "@tauri-apps/api": "^1.0.1",
    "next": "^12.2.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.0.2",
    "@types/node": "^18.0.1",
    "@types/react": "^18.0.14",
    "@types/react-dom": "^18.0.6",
    "eslint": "^8.19.0",
    "eslint-config-next": "^12.2.0",
    "typescript": "^4.7.4"
  }
}
